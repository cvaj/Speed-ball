# Functional Test Registry

Every Speed-ball capability must have functional proof. New capabilities must add or update a row before implementation is considered complete.

| Capability | Required Proof | Current Status |
|---|---|---|
| Root Gradle app skeleton | Wrapper checksum, dependency resolution, `:core:test`, `:app:testDebugUnitTest`, `:app:assembleDebug`, `:app:lintDebug`, root scripts, and `:core` boundary check | Phase 1 implemented locally; all listed proofs passed before implementation review |
| Placeholder no-read shell | JVM test asserts all workflow sections exist and no fake speed, frame rate, camera mode, or result value is exposed | Phase 1 implemented in `:app:testDebugUnitTest` |
| Camera2 high-speed HAL enumeration | Device/prototype log proving supported fps/res combos; app mapper tests for fixed vs blended ranges | Phase 4 app implementation approved and S10+ app HAL enumeration proven in `docs/HIGH_SPEED_FINDINGS.md`; S22+ pending |
| 120 fps burst capture | On-device burst with unique timestamp count, median gap in band, nonzero MP4, and clean release; decode reconciliation comes in Phase 5 | Phase 4 S10+ app proof passed for 720p@120; lifecycle-stop early burst correctly failed proof while releasing output |
| Decode/frame timestamp pairing | JVM tests for failure taxonomy, shared timestamp helper, exact-count reconciliation, metadata validators, bounded work helpers, cancellation gate, and no-result diagnostics; on-device S10+ proof for exact MP4 decode count, sensor count, gap histograms, PTS-vs-sensor offsets, and two sampled frames | Phase 5 code-level proof implemented; S10+ on-device run fail-loud proven with decoded `N=266`, unique sensor `M=320`, 1 ns near-duplicate sensor gaps, and exact-count reconciliation failed before pairing |
| Value-anchored timestamp reconciliation diagnostics | JVM tests for candidate generation, whole-frame shift ambiguity, residual gates, dropped-hole agreement, near-duplicate/post-collapse evidence, diagnostic `Proven` fixtures, no-measurement integration, logcat privacy, plus on-device S10+ `TIMESTAMP_ANCHOR_*` evidence | Phase 6 code/logging proof implemented. JVM tests prove synthetic diagnostic `Proven` behavior and that it still cannot become `DecodeOutcome.Success` or measurement-ready pairing. S10+ device proof captured `BURST_SUCCESS`, `TIMESTAMP_ANCHOR_*`, and `DECODE_*` logs; the measured verdict is `REJECTED reason=SENSOR_NEAR_DUPLICATE`, with decoded `N=268`, exact distinct sensor timestamps `325`, hypothetical post-collapse sensor count `260`, and no measurement-consumable proof |
| Record-then-decode source decision | JVM tests for timestamp-source characteristic mapping, path-free/no-measurement log formatting, and count-independent PTS-to-sensor value-match success/partial/ambiguity diagnostics; on-device S10+ bounded `SENSOR_INFO_TIMESTAMP_SOURCE` and `DECODE_PTS_SENSOR_VALUE_MATCH` logs; scoped mechanism assessment over encoder-frame-loss, sensor-callback-duplication, and REALTIME value-match branches | Phase 12 S10+ `1280x720 @ 120` record-then-decode route is scoped no-go for production measurement. Device proof logged `source=REALTIME value=1 sharedClockCandidate=true`; a fresh count-independent value-match logged `verdict=AMBIGUOUS_MATCH matched=248/257 ambiguous=62 longestCleanRun=3`, so the route still cannot recover a measurement-safe sensor-timestamp identity. No token, mph, angle, or trajectory is emitted. |
| Preview timestamp proof | JVM tests for preview failure taxonomy, bounded diagnostics, pure exact-membership pairer, cadence/drop rejection, edge trim bounds, nonzero/whole-frame offset rejection, UI no-result separation, and S10+ on-device preview proof logs | Phase 7 code-level proof implemented. S10+ preview-only constrained-high-speed session accepted and request list size was `4`; `SurfaceTexture.timestamp` exactly matched `SENSOR_TIMESTAMP` for consumed frames, but preview cadence was `33.3775 ms` at requested 120 fps, so the measured verdict is `PREVIEW_CADENCE_MISMATCH` with no measurement-consumable proof |
| Direct timing-source proof model | JVM tests for direct proof failure taxonomy including `INSUFFICIENT_DIRECT_FRAMES`, sequence content identity, optional detection hash tightening, token-eligibility consistency, path-free/pixel-free diagnostics, pixel-signature invariants, exact timestamp membership, sub-microsecond zero-offset equivalent, wrong-by-k rejection, preview-only cadence regression, timestamp error paths, deterministic aggregate pixel signatures, raw-pixel privacy, timestamp/signature count matching, resource caps, blank/stale rejection, inter-frame variation rejection, app-private companion scratch path, bounded scratch size, cleanup success/failure, no extractor/log path in the helper, direct frame late-callback/resource gates, release failure priority, release-step diagnostics, direct target above the token minimum, no one-frame stop, 12 off-band no-token behavior, 24-frame degraded-stream whole rejection, direct source scan for same-update `updateTexImage()`/timestamp/`glReadPixels` readback without decoder APIs, PBO readback source scan for GLES3 pixel-pack buffering, companion-first runner order, preview-control sequencing after teardown/deletion, scratch/camera hard-stop behavior, diagnostic-only preview control, minimum direct-frame gate before token eligibility, no encoded-file read in the runner, exactly one main-source timing-proof implementation, private direct-token construction, no caller-settable sequence proof identity, guard coverage for the live mint-chain symbols, rejection of naked direct tokens on the generic pipeline, rejection of genuine proof paired with an unrelated measured sequence, bound direct measurement input success path only when RGB frames recreate the proof-frame signatures, direct `autoStartDirectProof120` capture wiring, companion+direct high-speed surface shape, Phase 12 direct-source variant model, GL surface-order controls, producer-cadence interpretation gate, ImageReader same-acquired-image pixel proof contract, PRIVATE/video-encode ImageReader variant wiring, and no-read/path-free direct UI diagnostics | Phase 12 code now models baseline, direct-first, PBO-backed companion GL, direct-only GL, constrained YUV ImageReader, constrained PRIVATE/video-encode ImageReader, and standard ImageReader variants; GL and ImageReader capture wiring use selected surface roles; diagnostics carry variant, acquire-null, and producer-cadence interpretation fields; ImageReader pixel proof is tested as a same-snapshot contract for YUV images. S10+ Phase 12 device proof records constrained YUV ImageReader rejection, constrained PRIVATE ImageReader accepted but callback-empty and below producer band, standard ImageReader accepted but below the requested producer band, inline GL variants still failing loud, and `pbo-gl-readback` accepted with in-band producer cadence but still failing loud at about 30 fps consumed cadence (`DIRECT_CADENCE_MISMATCH`). |
| Phase 10 workflow state | JVM reducer tests for missing calibration/color/source, developer proof diagnostics staying separate, clearing prerequisites after success, no synthesized success without `MeasurementRunOutcome.Success`, and no main-source synthetic/fake timing proof scan | Phase 10 implemented locally; targeted `MeasurementWorkflowStateTest` passed |
| Calibration workflow UI state | JVM tests for finite positive calibration, zero/negative/non-finite distance rejection, non-finite/identical point rejection, revision carry-through, clearing readiness, and no result-value UI words | Phase 10 implemented locally; targeted `CalibrationWorkflowStateTest` and shell tests passed |
| Color sampling workflow UI state | JVM tests for HSV hue wraparound, tolerance clamping, ROI clipping, missing/invalid sample, invalid ROI, frame-bound validation, and sample/preview text without mph-like/result/confidence language | Phase 10 implemented locally; targeted `ColorWorkflowStateTest` and shell tests passed |
| Phase 10 result presentation | JVM tests for no-read reason/action text without values, success formatting only from `MeasurementRunOutcome.Success`, evidence label/detection count/trajectory fields, unproven timing no-read, long-message compaction, and source scan preventing calibration/color readiness imports | Phase 10 implemented locally; targeted `MeasurementResultUiStateTest` and shell tests passed |
| Phase 10 end-to-end JVM workflow fixture | JVM fixture creates synthetic RGB frames, runs sampled color, ROI, calibration, detector, measurement pipeline, and result formatting with test-source `SyntheticWorkflowTimingProof`; negative cases cover bad calibration, bad color/ROI, insufficient detections, unproven timing, and non-finite timestamps | Phase 10 implemented locally; targeted `MeasurementWorkflowFixtureTest` passed |
| Phase 13 S10+ visual estimate mode | JVM tests for typed estimate-vs-strict-measurement separation, non-finite/no-assumption no-reads, real timestamp conversion and nominal-cadence rejection, golden estimate from non-uniform timestamps, displacement-vs-timestamp skipped/coalesced interval inference, visual frame-delta inference with known per-frame interval when timestamps are unavailable, first-class visual-delta absolute-scale assumption disclosure, known-ball-diameter self-calibration scale disclosure/no-read coverage, motion-blurred blob short-axis self-calibration coverage, setup-time IMU level roll correction of launch angle, stationary launch-reference trimming, directional one-way candidate selection through static decoys, high-speed straight hit-window selection over slow same-color motion, leading empty-frame skip before ball entry, pitch/toss-like curved path no-read, minimum hit-speed no-read, missing-interval no-read, incoherent interval no-read, calibration/timestamp/residual/depth no-reads, real RGB frame-to-blob-to-estimate integration, bounded frame caps, direct live ARGB readback adapter validation/source scans, single-surface direct GL/readback guards, unique sensor timestamp diagnostics, direct failure taxonomy guards, estimator-owned detector trace/proof thumbnail bounds, zero-candidate proof summaries, terminal outcome proof source guards, and estimate UI labels/diagnostics/no-read value suppression | Phase 13/16 estimate implementation is locally tested. Targeted `MeasurementContractsTest`, `VisualEstimatePipelineTest`, `VisualEstimateFramePipelineTest`, `VisualEstimateCaptureProofTest`, `MeasurementResultUiStateTest`, `DirectTimingSourceCaptureTest`, `DirectVisualEstimateCaptureTest`, and import pipeline tests pass. Current direct estimate source guards require a setup preview surface before capture, keep the single hidden GL/readback surface as the only estimate frame source, report `Live feed required` when setup preview is missing, map direct session/resource failures to no-read without stale values, return bounded attempt-scoped proof thumbnails/counts from direct outcomes, and log frame/callback/readback counts plus unique `SENSOR_TIMESTAMP`, candidate frame/blob, and selected-sample counts. S10+ device safety smoke passed; the S10+ preview-plus-GL/readback direct-estimate two-surface shape is recorded as a measured no-go, and ground-truth speed validation remains open. |
| Phase 14 user calibration/capture workflow | JVM tests for normalized preview/readback coordinate mapping, anisotropic/aspect-ratio handling, letterbox/crop tap rejection, forward normalized-to-view preview mapping, 90-degree sensor-rotated preview round-trip, display-rotation live preview transform, pillarboxed caliper tap-to-calibration pixels-per-foot correctness, stale geometry invalidation, capture readiness reducer transitions, live IMU level setup updates that compensate true horizontal/vertical and stop/freeze at capture start, retry/recalibrate clearing, self-cal fallback subordination, shell setup assumption/no-read value suppression, visible setup-adjustment target state, user-entered distance parsing, distance-edit overwrite with A/B preservation, invalid-distance live/import stale-value rejection, visible calibration-plane/ball-plane depth inputs, visible motion-blob shape-ratio input, visible Impact-zone/Ball Box setup state, app-level Setup/Run mode state, Run Mode manual shoot/degraded voice state, idle no-match/speech-timeout green continuous-listening source guards, bounded fatal speech-recognizer retry/degrade source guards, narrow voice start-command allowlist guards for `shoot`/`record`/`cheese`/`smile`, voice `clear` result dismissal plus 10-second auto-clear source guards, attempt-scoped visual-estimate report identity/dismissal, cleared-report non-resurrection source guards, success-overlay shoot blocking source guards, no-read/failure bottom-panel next-shot allowance, live Camera2 feed source scan, explicit aspect-preserving `TextureView` transform source scan, active-target-only draggable vertical caliper line source scan, Captain-style continuous caliper fraction math, stable pointer-input/local active-drag override source scan, visible drawer status source scan, audio-first recorded-HFR route source scan with Ready cue after audio and video anchors, specific `VOICE_SHOOT_NOT_READY` readiness-reason source scan, post-capture live-feed recovery source guard, deterministic debug auto-start setup/level/retry source guards, success-status-gated full-screen result overlay source scan, no-read/failure bottom-panel no-blackout source scan, active capture status overlay source scan, estimate carry-distance formatting test, two-color IMU-only bottom-right level-reference-axis source scan, hidden translucent Tools handle source scan, single bottom drawer source scan, setup-as-drawer-page source scan, tap/drag gesture source scan, independent four-point Impact/Ball Box polygon source scans that reject rectangle/opposite-corner Ball Box resizing, PixelCopy live color sampling source scan, fail-closed cleared-color reducer behavior, IMU gravity/accelerometer/gyro source scan, and P14-G2-A end-to-end speed golden through real coordinate-to-calibration-to-estimate composition; S10+ UI smoke for overlay controls | Phase 14/16 workflow is implemented locally. Targeted `FrameCoordinateSpaceTest`, `CalibrationInputTest`, `LevelReferenceTest`, `DeviceLevelReferenceCaptureTest`, `Phase14WorkflowStateTest`, `SpeedBallShellStateTest`, `MeasurementResultUiStateTest`, and `CalibrationUiSourceTest` pass. The current setup UI is a landscape immersive live Camera2 feed with display-rotation `TextureView` preview transform and aspect-ratio-preserved layout, two draggable vertical A/B caliper lines, raw distance text that overwrites parsed feet while preserving A/B and blocks live/import when invalid, optional camera-to-calibration-plane and camera-to-ball-plane depth text for `DepthCorrected` estimate scaling, configurable motion-blob shape ratio defaulted to `2.00`, draggable independent-corner four-point Impact zone, setup-locked independent-corner four-point Ball Box size/color target, setup-only operation drawers, and explicit Run Mode with manual `Shoot`, truthful listening/degraded voice status, and a path back to Setup Mode. A Run Mode voice start command (`shoot`, `record`, `cheese`, or `smile`) creates a new attempt id, arms impact audio first, starts recorded HFR after the mic is listening, waits for the first video timestamp, emits Ready/beeps, and enables marker acceptance after cue completion maps to an audio sample index, and never requires setup reset between shots. Specific readiness reports/logs are shown when a start command is blocked. Android no-match/speech-timeout events restart listening while keeping the Run command state green and do not count as fatal voice failure; other speech-recognizer errors are bounded by one delayed scheduler, backoff, and a consecutive-error cap before manual-ready degradation. Success reports use a black full-screen overlay and accept voice `clear`, button `CLEAR`, or 10-second auto-clear before returning to Run listening; entering Setup also clears stale report state. No-read/failure reports use a non-fullscreen bottom panel with reason/action/message/proof imagery and no mph/angle/distance values and may coexist with the next ready/manual-ready state. `CLEAR` hides only that attempt report and setup/status/distance/live-feed/voice updates cannot resurrect it; a later same-reason no-read gets a new attempt id. Debug auto-start proof setup is reapplied after camera geometry is known and waits for a setup preview surface before consuming the pending direct-estimate trigger. Recorded capture may temporarily black the preview while camera ownership moves to the recorder, and completion/failure/no-read restarts the setup feed. The S10+ preview-plus-GL/readback two-surface direct-estimate session is recorded as a measured no-go. |
| IMU level sign validation | Physical S10+ gate: tilt phone to a known roll, verify cyan horizon overlay against true horizon, then measure known-horizontal motion and confirm corrected launch angle is approximately 0 degrees | `S10_IMU_LEVEL_SIGN_VALIDATION_PENDING`. JVM tests cover math validation, display-rotation mapping, provisional disclosure, and no-level disclosure, but they do not prove the physical sign convention. Until this gate closes, level-corrected launch angle is disclosed as provisional. |
| 240 fps GPU capture | On-device true 240 detections from preview/GPU path | Planned |
| HSV color calibration | Tap/sample -> HSV band -> mask preview/detection | Phase 8 foundation implemented in pure JVM tests for RGB-to-HSV goldens, hue wraparound, tolerance clamping, ROI clipping, finite validation, and processing-bound failures; Phase 10 adds pure workflow readiness state for sampled HSV/tolerance/ROI; Phase 16 samples a bounded patch from the live Camera2 feed with PixelCopy, converts the averaged pixel color to HSV, and fails closed if sampling is unavailable. |
| Ball centroid detection | Real frame fixtures produce centroids within tolerance | Phase 8 foundation implemented in pure JVM tests for connected-components centroid, no blob, two blobs, off-ROI blob, tiny blob, image-border hue wraparound, and noisy component-cap rejection; real-frame fixtures remain planned |
| Flight window selection | No read on insufficient/ambiguous detections; valid window on known clip | Phase 8 track extraction implemented in JVM tests for timestamp failures, insufficient detections, resolution bounds, jump rejection, interior miss handling, and no timestamp renumbering after dropped frames. Phase 16 adds JVM coverage for seeded one-directional motion-window selection that ignores unrelated color blobs, high-speed straight hit-window selection over slow same-color motion, leading empty-frame skip before ROI entry, curved pitch/toss rejection, and no-read below the configured hit-speed threshold. |
| Recorded-HFR window candidate-pipeline recovery | JVM regressions for recorded-HFR-scoped candidate/RANSAC budgets, unchanged sibling reducer behavior when no budget is configured, candidate-heavy windows that skip exhaustive RANSAC but still run directional centroid-path selection, runaway candidate-cap no-read, proof-only over-exposure no-read, source thumbnail proof without candidate detection, and recorded-HFR optional-ROI/full-frame source scans | 2026-06-07 implementation addresses field failure where S10+ capture/trigger selected the right 25-frame window but hung after `RECORDED_ESTIMATE_STAGE streaming window=true`. Device proof remains open: S10+ must show prompt terminal `RECORDED_ESTIMATE_WINDOW`, no GC hang, proof thumbnails, no mph on no-read, and re-arm for the next shot. |
| Recorded-HFR physical ball detector | JVM fixtures for temporal-persistence moving-ball isolation over static same-color background, same-ball fragment merge, sparse threshold-pixel HSV retention, compact box rejection via roundness, tilted capsule acceptance via solidity/PCA metrics, background-separation ambiguity, color-incoherent rejection, component-cap resource no-read, invalid-config no-read, single-frame background-jolt global-motion no-read, reducer selection of validated physical capsules over legacy shape penalties, post-reducer selected-track area collapse, edge-touching entry/exit exemption, PCA-axis-ratio swing rejection, adjacent HSV color jump rejection, and streaming integration proving physical candidates merge before recorded-HFR candidate budgets; source guard requires Run Mode sound-window route to opt into `RecordedHfrPhysicalDetectorConfig` while sibling paths remain default-off | Implemented locally for the sound-triggered recorded-HFR window route. The detector builds bit-packed threshold masks and bounded persistence counters, retains HSV only for threshold pixels, drops full ARGB after threshold/proof extraction, isolates moving same-color regions, merges fragments inside moving activity islands, emits physical candidates with shape/color metrics, validates selected-track shape/color consistency, and feeds existing RANSAC/path selection. S10+ field proof remains open: proof thumbnails and logs must show physical detector summaries and either a terminal estimate or a fail-loud physical detector reason. |
| Fixed-camera recorded-HFR motion detector | JVM tests for median-background luma foreground detection, no-foreground no-read, zero-candidate camera-view no-read/report text, global lighting/camera-motion no-read, oversized merged foreground as `BALL_NOT_ISOLATED`, selected-color child splitting from merged moving foreground, twenty-pixel speck rejection, Impact-zone masking before candidate caps, expected Ball Box size rejection of too-small/too-large moving junk, elongated motion-blur candidates surviving side-ratio scoring, too-thin streak rejection by minimum short side, multi-fragment foreground ranking by selected color plus near-circular shape before velocity/path selection, configurable close-ball bounds, color-independent compact moving-object detection, saved tennis impact-window PPM frames through the production Kotlin detector, manual extracted MP4-window proof through the streaming reducer/estimate path, selected-track proof thumbnails from the processed decoded window, bounded streaming integration, detector-config conflict no-read, stationary foreground rejection, non-directional/sawtooth path rejection, optional configured mph-floor rejection, low-resolution motion-scout selection of a high-centroid-travel and sufficient-mean-area interval over slower or weak-speck foreground motion, apparent blob-size variation not acting as a standalone veto when the trajectory/timing fit is otherwise coherent, diameter-scaled residual tolerance for motion-blurred blob centroids, Android source-level luma-scout guards before full ARGB conversion, and paired fast near-circular motion success through the real reducer/estimate pipeline | Implemented locally in `RecordedHfrMotionBallDetectorTest`, `RecordedHfrManualVideoTest`, `RecordedHfrStreamingEstimateTest`, `RecordedHfrWindowFrameSourceTest`, `VisualEstimateCaptureProofTest`, `VisualEstimateFramePipelineTest`, `VisualEstimatePipelineTest`, `Phase14WorkflowStateTest`, `MeasurementResultUiStateTest`, and `CalibrationUiSourceTest`. Run Mode recorded-HFR now opts into `RecordedHfrMotionDetectorConfig`, decodes the bounded impact window from 1 second before the accepted sound marker through 1 second after it, uses a source-level low-resolution luma scout to select a padded high-travel and sufficient-mean-area detector interval before full ARGB conversion, falls back to the full bounded window if the scout is unsure, rejects weak speck-only scout windows before they can crop away the ball, treats color/ROI as optional evidence rather than readiness gates, applies the optional user-drawn Impact zone before morphology/component counting, applies the optional setup-locked Ball Box size gate before candidate caps, splits selected-color child candidates out of larger moving parents, ranks multiple isolated motion fragments by selected color, configurable near-circular side ratio defaulted to `2.00`, compactness, size, and edge evidence before path reduction, lets side ratio reduce candidate score instead of vetoing motion-blurred streaks by itself, requires cadence-coherent high-velocity windows, uses centroid velocity and smooth one-directional flight path as discriminators, treats apparent blob-size variation as diagnostic evidence rather than a standalone no-read after the track/timing fit passes, scales fit residual tolerance from selected blob short-side diameter when available, keeps bounded thumbnails from the processed detector frames so selected ball-track frames are shown first in proof, reports zero retained motion candidates as no moving ball blobs in the camera frame view, disables final generic outlier pruning after recorded-HFR RANSAC window selection, preserves detector/fit confidence after recorded-HFR timing provenance has passed, leaves mph floors to optional configuration instead of a hard-coded Run Mode cutoff, and fails loud on source-invalid, no-foreground, merged foreground, or ambiguous tracks. Local proof against the previous 300 ms / 400 ms extracted S10+ MP4 window scanned 85 frames, retained 33 candidate frames / 47 candidate blobs, selected 8 samples, passed source validity, and produced a HIGH-confidence estimate around 18.83 mph / 15.29 deg from container PTS with 2.06 px residual; phone install/field repeat proof for the new 1-second / 1-second window remains required. |
| Velocity fit | Golden values for slope, speed, angle, residuals, outlier rejection | Phase 2 implemented in `:core:test`; canonical `hitting2` parity pending until fixture data exists |
| Distance calibration | Known pixel gap and real distance produce expected pixels/foot | Phase 2 implemented in `:core:test` |
| Unit conversion | px/s -> ft/s -> mph golden values | Phase 2 implemented in `:core:test` |
| Core measurement outcome | Detections + calibration -> typed success/failure; no partial mph/angle on failure | Phase 2 implemented in `:core:test` with synthetic end-to-end golden and invalid-options failure coverage |
| Drag/backspin trajectory | No-drag closed form, drag monotonicity, hand-calculated case, zero-spin preserves drag-only output, positive backspin extends carry/apex/hang, topspin shortens carry, and zero-air removes Magnus lift | Phase 3 implemented in `:core:test` with drag-zeroed RK4 goldens, interpolation, angle-edge, zero-speed, error-path coverage, and optional Nathan spin-parameter Magnus lift tests. Result UI keeps drag-only `carryFt` as the baseline and adds model-based `backspinCarryFt` without changing capture/decode/detection. |
| Results UI | Valid read and no-read states display correct user-facing messages | Phase 10 result-state formatting implemented in Compose-free JVM tests: production no-read exposes no mph/angle/trajectory values; success formatting is tested only with synthetic test-source timing proof or end-to-end JVM fixture proof |
| Import/recorded estimate mode | JVM tests for estimate-only import contracts, user-selected content URI validation, app-owned recorded-file frame source support, metadata rejection, bounded extraction/cancellation/resource release, monotonic PTS, known-frame-interval slow-motion timing, recorded-capture frame-interval timing with LOW MediaRecorder frame-drop/coalescing caveat, original-decoded-frame-index timing for retained recorded candidates, recorded-HFR metadata/scanned-count-vs-unique-`SENSOR_TIMESTAMP` gate, candidate-count-not-capture-count guards, recorded-HFR sensor-cadence gate, recorded-HFR 640x360 working-resolution selection, scaled area-gate contract, real-pipeline 720p-vs-640x360 scale-invariance golden with 5% tolerance and anti-halving assertion, AE-first/manual-exposure diagnostics, slow-AE manual cap source guard above the 12 ms blur-risk threshold, exposure blur-risk no-read guard, debug-only bounded failed-MP4 retention policy, streaming decode that discards full-frame ARGB after detection, scan bound/cancellation no-reads, retained-candidate cap no-read, no-read proof imagery sequencing before gate enforcement, compact/original proof thumbnail mapping, visual-delta timing reconciliation, imported/recorded RGB frames through the existing `VisualEstimateFramePipeline`, RANSAC-style straight-hit consensus source contract, source scan proving no import `MeasurementRunOutcome.Success`, source scan proving primary recorded-HFR avoids batch `ImportFrameExtractor.extract`, voice start-command trigger wiring with automatic level capture before app-owned recording, import workflow no-read value suppression, persisted redacted saved-result summaries, corrupt-store fail-loud behavior, and redacted import/recorded export evidence. Sound-triggered window coverage adds tests for impact-audio transient detection and `AudioRecord.getTimestamp()` anchoring, Android `AudioRecord` BOOTTIME/fallback-source route guards, audio/video anchor-to-container window mapping, bounded PTS-window frame source and validator failures, Android `MediaExtractor`/`MediaCodec` source scans, windowed capture gate with sensor/container count mismatch, external-stop window cadence acceptance without full planned-duration sensor-count proof, recorded container-PTS-delta timing goldens, missing/nonmonotonic PTS no-reads, source-validity black/near-black window no-read with proof thumbnails, optional window proof metadata, ByteBuffer YUV conversion coverage for tight/padded NV12 and I420 plus luma-only scout extraction, unsupported/NV21-like fail-loud, too-small buffers, non-1:1 1920x1080-to-640x360 downscale mapping, source guards forbidding `ImageReader`/`getOutputImage`/plane APIs and full-window frame lists, and Run Mode voice start-command routing to the sound-triggered window. | Phase 15/16 implemented locally for user-selected imported clips and voice-triggered app-owned recorded clips as estimate-only. Targeted `com.speedball.app.importing.*`, `VisualEstimateFramePipelineTest`, `VisualEstimatePipelineTest`, and `com.speedball.app.ui.*` tests pass. The HFR recorded-source correction adds `RecordedHfrStreamingEstimateTest`, `RecordedHfrEstimateContractsTest`, `RecordedHfrRetentionPolicyTest`, updated `BurstDiagnosticsTest`, `ImportContractsTest`, `ImportTimingReconcilerTest`, `VisualEstimateCaptureProofTest`, `RecordedHfrByteBufferYuvConverterTest`, and `CalibrationUiSourceTest` coverage for start-command routing to recorded-HFR, proof thumbnails from streamed recorded frames, scanned-vs-candidate count separation, original-index timing gaps, count/cadence no-read gates, caps/cancellation, 720p/1080p to 640x360 working resolution, AE-first capture that leaves normal 120 fps 8.33 ms exposure alone and only manually caps reported exposure above 12 ms, actual exposure min/median/max, coarse 12 ms motion-blur-risk no-read, debug retention caps, source-level luma scout before full ARGB conversion, and scale invariance without half-speed regression. Sound-triggered HFR window batch 2 is wired into Run Mode: speech start commands (`shoot`, `record`, `cheese`, or `smile`) and manual `Shoot` arm impact audio first, start HFR after the mic is listening, wait for the first positive capture-result `SENSOR_TIMESTAMP`, blank the Ready cue by audio sample index, accept any post-Ready loud ambient-relative spike including a mouth pop, map the pop marker to a bounded container-PTS decode window, use real Android `MediaExtractor`/`MediaCodec` byte-buffer decoding instead of whole-clip retriever/index extraction or render-surface plane APIs, feed the bounded window through container-PTS-delta estimate timing, and report black/near-black decoded windows as source-invalid before insufficient-detections. S10+ timestamp-source proof on 2026-06-07 logged `source=REALTIME value=1 sharedClockCandidate=true`; S10+ ByteBuffer proof on 2026-06-07 measured `COLOR_FormatYUV420SemiPlanar`/NV12 with `stride=1280`, `sliceHeight=720`, then source-drained 24 frames from `0..191433 us` with an empty app crash buffer. End-to-end window-on-ball device proof and black/drop regression remain required before strict production accuracy can be claimed. Android S10+ `hitball1.mp4` import fixture smoke produced a labeled LOW-confidence known-distance estimate using 11 ft marker calibration, a declared 120 fps interval, speckle filtering, and the high-velocity straight hit-window selector: `41.3 mph`, `24.3 deg`, `9` frames, `0.56 px` residual. This is still estimate-only, not strict production measurement proof. |
| Streaming impact-audio trigger and HFR failsafe | JVM tests for streaming-vs-batch impact-audio equivalence including identical verdict, detected sample index, scalar diagnostics, phase gates, cue blanking, invalid boundaries, chunk boundaries, real-device delta-around-100 trigger, near-silence rejection, moderate-ambient sub-pop rejection, and work-counter/no-starvation behavior; Android worker source guard rejects `copyOf(written)` and requires `ImpactAudioStreamingDetector`; burst duration helper tests prove `BurstStopMode.ExternalStop` still has a hard cap; source guards keep Run Mode `shoot` on audio-first recorded-HFR. Device proof must show multi-pop reliability, realistic outdoor/noisy ambient no-false-trigger, prompt no-impact, and bounded HFR recording. | Implemented locally in `ImpactAudioTriggerTest`, `BurstDiagnosticsTest`, `RecordedHfrWindowFrameSourceTest`, and `ImportContractsTest`. The S10+ still needs final field proof after install. |
