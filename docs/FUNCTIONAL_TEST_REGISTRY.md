# Functional Test Registry

Every Speed-ball capability must have functional proof. New capabilities must add or update a row before implementation is considered complete.

| Capability | Required Proof | Current Status |
|---|---|---|
| Root Gradle app skeleton | Wrapper checksum, dependency resolution, `:core:test`, `:app:testDebugUnitTest`, `:app:assembleDebug`, `:app:lintDebug`, root scripts, and `:core` boundary check | Phase 1 implemented locally; all listed proofs passed before implementation review |
| Placeholder no-read shell | JVM test asserts all workflow sections exist and no fake speed, frame rate, camera mode, or result value is exposed | Phase 1 implemented in `:app:testDebugUnitTest` |
| Camera2 high-speed HAL enumeration | Device/prototype log proving supported fps/res combos; app mapper tests for fixed vs blended ranges | Phase 4 app implementation approved and S10+ app HAL enumeration proven in `docs/HIGH_SPEED_FINDINGS.md`; S22+ pending |
| 120 fps burst capture | On-device burst with unique timestamp count, median gap in band, nonzero MP4, and clean release; decode reconciliation comes in Phase 5 | Phase 4 S10+ app proof passed for 720p@120; lifecycle-stop early burst correctly failed proof while releasing output |
| Decode/frame timestamp pairing | JVM tests for failure taxonomy, shared timestamp helper, exact-count reconciliation, metadata validators, bounded work helpers, cancellation gate, and no-result diagnostics; on-device S10+ proof for exact MP4 decode count, sensor count, gap histograms, PTS-vs-sensor offsets, and two sampled frames | Phase 5 code-level proof implemented; S10+ on-device run fail-loud proven with decoded `N=266`, unique sensor `M=320`, 1 ns near-duplicate sensor gaps, and exact-count reconciliation failed before pairing |
| Value-anchored timestamp reconciliation diagnostics | JVM tests for candidate generation, whole-frame shift ambiguity, residual gates, dropped-hole agreement, near-duplicate/post-collapse evidence, diagnostic `Proven` fixtures, no-measurement integration, logcat privacy, plus on-device S10+ `TIMESTAMP_ANCHOR_*` evidence | Phase 6 code/logging proof implemented. JVM tests prove synthetic diagnostic `Proven` behavior and that it still cannot become `DecodeOutcome.Success` or measurement-ready pairing. S10+ device proof captured `BURST_SUCCESS`, `TIMESTAMP_ANCHOR_*`, and `DECODE_*` logs; the measured verdict is `REJECTED reason=SENSOR_NEAR_DUPLICATE`, with decoded `N=268`, exact distinct sensor timestamps `325`, hypothetical post-collapse sensor count `260`, and no measurement-consumable proof |
| Preview timestamp proof | JVM tests for preview failure taxonomy, bounded diagnostics, pure exact-membership pairer, cadence/drop rejection, edge trim bounds, nonzero/whole-frame offset rejection, UI no-result separation, and S10+ on-device preview proof logs | Phase 7 code-level proof implemented. S10+ preview-only constrained-high-speed session accepted and request list size was `4`; `SurfaceTexture.timestamp` exactly matched `SENSOR_TIMESTAMP` for consumed frames, but preview cadence was `33.3775 ms` at requested 120 fps, so the measured verdict is `PREVIEW_CADENCE_MISMATCH` with no measurement-consumable proof |
| Direct timing-source proof model | JVM tests for direct proof failure taxonomy including `INSUFFICIENT_DIRECT_FRAMES`, sequence content identity, optional detection hash tightening, token-eligibility consistency, path-free/pixel-free diagnostics, pixel-signature invariants, exact timestamp membership, sub-microsecond zero-offset equivalent, wrong-by-k rejection, preview-only cadence regression, timestamp error paths, deterministic aggregate pixel signatures, raw-pixel privacy, timestamp/signature count matching, resource caps, blank/stale rejection, inter-frame variation rejection, app-private companion scratch path, bounded scratch size, cleanup success/failure, no extractor/log path in the helper, direct frame late-callback/resource gates, release failure priority, release-step diagnostics, direct target above the token minimum, no one-frame stop, 12 off-band no-token behavior, 24-frame degraded-stream whole rejection, direct source scan for same-update `updateTexImage()`/timestamp/`glReadPixels` readback without decoder APIs, companion-first runner order, preview-control sequencing after teardown/deletion, scratch/camera hard-stop behavior, diagnostic-only preview control, minimum direct-frame gate before token eligibility, no encoded-file read in the runner, exactly one main-source timing-proof implementation, private direct-token construction, no caller-settable sequence proof identity, guard coverage for the live mint-chain symbols, rejection of naked direct tokens on the generic pipeline, rejection of genuine proof paired with an unrelated measured sequence, bound direct measurement input success path only when RGB frames recreate the proof-frame signatures, direct `autoStartDirectProof120` capture wiring, companion+direct high-speed surface shape, and no-read/path-free direct UI diagnostics | Phase 11 code targets 24 direct readbacks, preserves no-token behavior below 12 whole-clean frames, and logs bounded root-cause diagnostics. S10+ device proof reached 6 direct readbacks, sensor callbacks at 120 fps, direct consumed cadence at ~30 fps, scratch cleanup success, and final fail-loud `INSUFFICIENT_DIRECT_FRAMES`. |
| Phase 10 workflow state | JVM reducer tests for missing calibration/color/source, developer proof diagnostics staying separate, clearing prerequisites after success, no synthesized success without `MeasurementRunOutcome.Success`, and no main-source synthetic/fake timing proof scan | Phase 10 implemented locally; targeted `MeasurementWorkflowStateTest` passed |
| Calibration workflow UI state | JVM tests for finite positive calibration, zero/negative/non-finite distance rejection, non-finite/identical point rejection, revision carry-through, clearing readiness, and no result-value UI words | Phase 10 implemented locally; targeted `CalibrationWorkflowStateTest` and shell tests passed |
| Color sampling workflow UI state | JVM tests for HSV hue wraparound, tolerance clamping, ROI clipping, missing/invalid sample, invalid ROI, frame-bound validation, and sample/preview text without mph-like/result/confidence language | Phase 10 implemented locally; targeted `ColorWorkflowStateTest` and shell tests passed |
| Phase 10 result presentation | JVM tests for no-read reason/action text without values, success formatting only from `MeasurementRunOutcome.Success`, evidence label/detection count/trajectory fields, unproven timing no-read, long-message compaction, and source scan preventing calibration/color readiness imports | Phase 10 implemented locally; targeted `MeasurementResultUiStateTest` and shell tests passed |
| Phase 10 end-to-end JVM workflow fixture | JVM fixture creates synthetic RGB frames, runs sampled color, ROI, calibration, detector, measurement pipeline, and result formatting with test-source `SyntheticWorkflowTimingProof`; negative cases cover bad calibration, bad color/ROI, insufficient detections, unproven timing, and non-finite timestamps | Phase 10 implemented locally; targeted `MeasurementWorkflowFixtureTest` passed |
| 240 fps GPU capture | On-device true 240 detections from preview/GPU path | Planned |
| HSV color calibration | Tap/sample -> HSV band -> mask preview/detection | Phase 8 foundation implemented in pure JVM tests for RGB-to-HSV goldens, hue wraparound, tolerance clamping, ROI clipping, finite validation, and processing-bound failures; Phase 10 adds pure workflow readiness state for sampled HSV/tolerance/ROI; tap UI remains planned |
| Ball centroid detection | Real frame fixtures produce centroids within tolerance | Phase 8 foundation implemented in pure JVM tests for connected-components centroid, no blob, two blobs, off-ROI blob, tiny blob, image-border hue wraparound, and noisy component-cap rejection; real-frame fixtures remain planned |
| Flight window selection | No read on insufficient/ambiguous detections; valid window on known clip | Phase 8 track extraction implemented in JVM tests for timestamp failures, insufficient detections, resolution bounds, jump rejection, interior miss handling, and no timestamp renumbering after dropped frames; real clip windowing remains planned |
| Velocity fit | Golden values for slope, speed, angle, residuals, outlier rejection | Phase 2 implemented in `:core:test`; canonical `hitting2` parity pending until fixture data exists |
| Distance calibration | Known pixel gap and real distance produce expected pixels/foot | Phase 2 implemented in `:core:test` |
| Unit conversion | px/s -> ft/s -> mph golden values | Phase 2 implemented in `:core:test` |
| Core measurement outcome | Detections + calibration -> typed success/failure; no partial mph/angle on failure | Phase 2 implemented in `:core:test` with synthetic end-to-end golden and invalid-options failure coverage |
| Drag trajectory | No-drag closed form, drag monotonicity, hand-calculated case | Phase 3 implemented in `:core:test` with drag-zeroed RK4 goldens, interpolation, angle-edge, zero-speed, and error-path coverage |
| Results UI | Valid read and no-read states display correct user-facing messages | Phase 10 result-state formatting implemented in Compose-free JVM tests: production no-read exposes no mph/angle/trajectory values; success formatting is tested only with synthetic test-source timing proof or end-to-end JVM fixture proof |
| Import mode | Imported clip runs same detector/math pipeline | Planned |
