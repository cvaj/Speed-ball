Your stupid application is written so fucking stupid, it's not even funny. How the fucking app should work is:
1) setup mode
2) run mode

setup is where you initially setup all your parameters for it to run. This is 100% independent of running. 
run node is where you actually run the application. Nothing should be set at this point. It's simply listening for commands to execut and nothing more. All setups should already be done. I don't want to have to set it up every time I want it to shooot something. Once all settings are set, it should have a status button(green for ready) and once that's green, it's in listening mode. It might want to provide an audible beep every 2 second so the user knows it's listening. Once the user says "shoot", the app fires capturing a ball moving across the screen and then it computes using the images. That's it. Is this so fucking hard? when don analyzing, it computes and reports. 